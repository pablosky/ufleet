<%- model_class = Driver -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>

      <th><%= model_class.human_attribute_name(:cargo) %></th>

      <th><%= model_class.human_attribute_name(:ci) %></th>

      <th><%= model_class.human_attribute_name(:nombre) %></th>

      <th><%= model_class.human_attribute_name(:apellidopaterno) %></th>

      <th><%= model_class.human_attribute_name(:apellidomaterno) %></th>

      <th><%= model_class.human_attribute_name(:celular) %></th>

      <th><%= model_class.human_attribute_name(:estadochofer) %></th>

      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @drivers.each do |driver| %>
      <tr>
        <td><%= link_to driver.id, driver_path(driver) %></td>

        <td><%= driver.cargo %></td>

        <td><%= driver.ci %></td>

        <td><%= driver.nombre %></td>

        <td><%= driver.apellidopaterno %></td>

        <td><%= driver.apellidomaterno %></td>

        <td><%= driver.celular %></td>

        <td><%= driver.estadochofer %></td>

        <td><%=l driver.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_driver_path(driver), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      driver_path(driver),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_driver_path,
            :class => 'btn btn-primary' %>
